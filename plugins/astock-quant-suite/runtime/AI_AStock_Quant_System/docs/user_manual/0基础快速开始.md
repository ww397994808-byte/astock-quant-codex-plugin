# 0基础快速开始

```bash
python3 scripts/install_astock_skill.py --force
python3 cli.py student-workflow --idea "中国神华日线布林低吸波段，控制回撤，不要太频繁交易" --timeframe 1d --adjust point_in_time_qfq --auto-refine
```

结果在 `reports/run_id/`。先看 `STUDENT_WORKFLOW_SUMMARY.md`，如果报告显示 `INVALID`，再看 `NEXT_ACTIONS.md`，不要进入模拟盘或实盘。
